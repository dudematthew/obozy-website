# Vue CLI exposes only VUE_APP_* to the client bundle.
# Local: PHP quest API on 8090 (Vue stays on default 8080).
VUE_APP_API_BASE_URL=http://127.0.0.1:8090

# Production build uses .env.production by default.
# VUE_APP_API_BASE_URL=https://festiwal.dudematthew.smallhost.pl
